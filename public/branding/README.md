# Branding Assets

Replace these files with your final branding artwork:

- `app-logo.svg`: used in the navbar.
- `app-icon.svg`: used as the browser tab icon (favicon).

Keep the same filenames so no code changes are needed after upload.
